# HANDOFF — PackageBuilder

> **این فایل برای شروعِ یک چتِ تازه است.** همه‌چیزی که یک دستیارِ نو لازم دارد
> تا بی‌پرسش‌وپاسخِ اضافی کار را ادامه دهد، اینجاست: چه ساخته شده، چرا این‌طور
> ساخته شده، چه باگ‌هایی خورده‌ایم و چطور حل شدند، و مرزهایی که نباید رد شوند.
>
> - **مسیرِ پروژه:** `D:\- AIProject\26- PackageBuilder`
> - **آخرین کامیت:** `bb69edc` — «تأییدِ نهایی: هر ۱۹ تکنولوژی واقعاً اجرا شد»
> - **وضعیت:** هر ۸ قدمِ نقشه تمام. ۲۰۵ تستِ واحد + ۷ تستِ سرتاسری، همه سبز.
> - **پلتفرم:** ویندوز ۱۱، PowerShell 7، Node ۲۴، Docker Desktop
> - **زبانِ کار:** فارسی. کدها و کامنت‌ها و اسناد همه فارسی‌اند.

---

## ۱. این ابزار چه کاری می‌کند

یک پوشهٔ خالی می‌گیرد و قدم‌به‌قدم — با انتخابِ کاربر، زیرِ نظرِ او — تبدیلش
می‌کند به پروژه‌ای با سرویس‌های **واقعاً کارکننده**. و می‌تواند هر تصمیم را
پس هم بگیرد.

سه کارِ اصلی:

1. **اسکلتِ قدمِ صفر** — پوشه‌ای که هیچ تکنولوژی‌ای را فرض نمی‌گیرد.
2. **گزارشِ وضعیتِ واقعی** — با مدرک، سه‌حالتی (هست / نیست / **نامعلوم**).
3. **اعمال و برگشتِ تصمیم‌ها** — CLIِ رسمیِ هر تکنولوژی در یک ترمینالِ
   PowerShellِ واقعیِ قابلِ دیدن، به‌همراهِ لایهٔ چسبِ خودمان.

---

## ۲. چرا از صفر نوشته شد

نسخهٔ قبلی (`25- TaminLibrary/tools/project-creator`) روی یک قالبِ آمادهٔ
تک‌استک ساخته شده بود و بعد سعی می‌کرد عمومی شود. سه مشکلِ ریشه‌ای داشت:

| مشکل | جزئیات |
|---|---|
| پوشهٔ نوساخته پُر بود از تصمیم‌های گرفته‌نشده | `package.json` و `turbo.json` و `docker-compose.yml` از اول بودند، یعنی زبان و مونوریپو و دیتابیس بی‌انتخابِ کسی انتخاب شده بودند |
| «اعلام‌شده» با «واقعی» اشتباه می‌شد | نامِ پکیج در `package.json` سبز نشان داده می‌شد بی‌آنکه چیزی نصب باشد. این خطا **سه دورِ جدا** تکرار شد: پکیج‌های npm، پکیج‌های ریشه، و سرویس‌های Docker (که از متنِ yaml خوانده می‌شد نه از خودِ Docker) |
| خودکارسازیِ بی‌نظارت | کارها پشتِ صحنه اجرا می‌شدند و فقط لاگِ خلاصه دیده می‌شد |

**ریشهٔ همهٔ سبزهای دروغین:** نسخهٔ قبلی حالتِ «نامعلوم» نداشت و «نتوانستم
بپرسم» را «نصب است» ترجمه می‌کرد.

---

## ۳. شش قاعدهٔ سخت — قابلِ مذاکره نیستند

اینها دلیلِ وجودِ بازنویسی‌اند. شرحِ کامل در
`docs/decisions/0001-purpose-and-rules.md`.

1. **بی‌مدرک ادعا نکن.** «نصب است» فقط با مدرکِ واقعی: پوشه در
   `node_modules`، خروجیِ `docker compose ps`، وجودِ `.venv`. حضورِ نام در یک
   فایلِ کانفیگ **مدرک نیست**. سه حالت، نه دو. «نامعلوم» هرگز به «هست» یا
   «نیست» ترجمه نمی‌شود.
2. **هیچ چیزی پنهان اجرا نشود.** هر دستور در ترمینالِ واقعیِ قابلِ دیدن، با
   خروجیِ کاملِ خودش. بدونِ لاگِ شبیه‌سازی‌شده.
3. **هیچ تصمیمی تحمیل نشود.** فایلِ یک تکنولوژی، فقط بعد از انتخابِ آن.
4. **هر کاری دو طرفه باشد.** هرچه نصب می‌شود باید حذف هم بشود، و بعدِ حذف
   باید در فهرست **بماند** (به‌صورتِ نصب‌نشده) تا بشود دوباره نصبش کرد.
5. **قبل از گفتنِ «انجام شد»، تست شده باشد** — از جمله حالتِ خالی و خطا.
6. **خودبسنده باش.** هیچ وابستگی به پوشه، پروژه، یا مسیرِ خاصی. هیچ مسیرِ
   مطلق و هیچ نامِ پروژهٔ همسایه‌ای در کد نمی‌آید.

قاعدهٔ ۶ **به‌عنوانِ اصلاحیه** اضافه شد، چون کاربر دید که ابزار در متنِ راهنما
به `25- TaminLibrary` اشاره کرده بود: «من نمیخوام همش گیر پوشه ۲۵ باشما… این
برنامه می‌خوام مستقل از هر فایل و پوشه‌ای باشه.» الان
`tests/independence.test.mjs` اجبارش می‌کند، نه فقط ادعا.

---

## ۴. ساختارِ فایل‌ها

```
26- PackageBuilder/
├── CLAUDE.md                       دستورالعملِ خودِ این پوشه
├── README.md                       معرفی + جدولِ امنیت + قواعد
├── HANDOFF.md                      همین فایل
├── package.json                    ۴ وابستگیِ اصلی + ۱ توسعه
├── playwright.config.mjs
├── docs/
│   ├── ROADMAP.md                  ⭐ هشت قدم + تأییدِ نهایی، با جدولِ هر باگ
│   └── decisions/
│       ├── 0001-purpose-and-rules.md            قواعدِ سخت + اصلاحیهٔ قاعدهٔ ۶
│       ├── 0002-tool-stack.md                   چرا Node/node-pty/xterm
│       └── 0003-how-a-technology-is-applied.md  CLIِ رسمی + چسب + اصلاحیهٔ برگشت
├── src/
│   ├── cli.mjs                     (۳۴۹) new | probe | stack | apply | revert | serve
│   ├── core/
│   │   ├── skeleton.mjs            (۶۹)  قالب‌ها + جای‌گیرها
│   │   ├── scaffold.mjs            (۱۷۹) ساختِ پوشهٔ نو + git init
│   │   ├── detect.mjs              (۳۷۰) تشخیصِ سه‌حالتی، فقط‌خواندنی
│   │   ├── registry.mjs            (۷۵۱) ⭐ ۱۰ دسته، ۱۹ تکنولوژی — داده، نه کد
│   │   ├── resolve.mjs             (۲۱۸) ارزیابیِ مدرک‌ها روی پروژهٔ واقعی
│   │   ├── apply.mjs               (۸۸۹) ⭐ اعمال و برگشت
│   │   ├── git.mjs                 (۱۳۵) کامیتِ هر تصمیم
│   │   └── templates/              ۱۳ فایلِ اسکلت (فایلِ واقعی، نه رشته در کد)
│   └── server/
│       ├── server.mjs              (۳۲۷) node:http + WebSocket + محافظت
│       ├── terminal.mjs            (۲۲۱) PowerShell 7ِ همیشه‌زنده
│       ├── sentinel.mjs            (۱۸۲) تشخیصِ «فرمان تمام شد» — عمداً خالص
│       └── public/index.html       (۵۹۵) کلِ UI، بدونِ مرحلهٔ بیلد
├── tests/                          ۸ فایل، ۲۰۵ تست
└── e2e/build-a-project.spec.mjs    ۷ تستِ مرورگرِ واقعی
```

اسکلتی که برای هر پروژهٔ نو ساخته می‌شود، ۱۳ فایل است و **هیچ‌کدام تکنولوژی
تحمیل نمی‌کند**: `README.md`, `CLAUDE.md`, `.gitignore`, `.gitattributes`,
`.editorconfig`, `.env.example`, `project.config.json`,
`docs/README.md`, `docs/decisions/README.md`,
`docs/decisions/0001-open-decisions.md`, `deployment/README.md`,
`scripts/README.md`, `.github/workflows/README.md`.

### فرمان‌ها

```bash
node src/cli.mjs new ./my-app --name "My App"     # ساختِ اسکلت (--dry-run هم دارد)
node src/cli.mjs probe ./my-app                   # وضعیتِ واقعی، فقط‌خواندنی
node src/cli.mjs stack ./my-app                   # تصمیم‌ها و گزینه‌ها
node src/cli.mjs apply ./my-app --tech postgres   # اعمال (--dry-run هم دارد)
node src/cli.mjs revert ./my-app --tech postgres  # برگشت
node src/cli.mjs serve                            # UI روی 127.0.0.1:4600
npm test                                          # ۲۰۵ تست، ~۲۰ ثانیه
npm run e2e                                       # ۷ تستِ Playwright
```

`new` روی پوشهٔ پُر **امتناع** می‌کند و دست نمی‌زند (زبالهٔ سیستم‌عامل مثلِ
`desktop.ini` نادیده گرفته می‌شود). `probe` هیچ‌چیزی نمی‌نویسد و سه حالت را
با `✓` / `✗` / `؟` تفکیک می‌کند.

---

## ۵. تصمیم‌های معماریِ اصلی

### الف) استکِ خودِ ابزار (تصمیمِ ۰۰۰۲، تأییدِ صریحِ کاربر)

| بخش | انتخاب | دلیل |
|---|---|---|
| رانتایم | Node.js | `node-pty` تنها راهِ عملیِ PTYِ واقعیِ ویندوزی است |
| ترمینالِ سرور | `node-pty` | باینریِ آماده، بی‌نیاز به کامپایل |
| ترمینالِ مرورگر | `xterm.js` + `addon-fit` | همان چیزی که VS Code دارد |
| پُل | WebSocket (`ws`) | خروجیِ ترمینال جریانی است |
| سرورِ HTTP | خودِ `node:http` | چند مسیرِ ساده؛ فریم‌ورک فقط وابستگی است |
| UI | HTML/CSS/JSِ ساده، **بدونِ بیلد** | ابزارِ لوکالِ تک‌کاربره؛ بیلد یعنی هر تغییرِ کوچک یک چرخهٔ کامپایل |
| تست | Playwright | تصمیمِ کاربر: یک‌بار تستِ سرتاسری، جای چک‌کردنِ دستیِ مرورگر |

وابستگی‌ها فقط ۵ موردند: `node-pty`, `ws`, `@xterm/xterm`,
`@xterm/addon-fit`, `@playwright/test`. قدم‌های ۲ تا ۴ **صفر** وابستگی
داشتند و Playwright دو بار عمداً عقب افتاد (بارِ اول چون قدمِ ۴ صفر-وابستگی
بود، بارِ دوم چون ساختارِ صفحه در قدم‌های ۷ و ۸ عوض می‌شد و تستِ e2e دو بار
بازنویسی می‌شد).

### ب) یک تکنولوژی چطور اعمال می‌شود (تصمیمِ ۰۰۰۳، تأییدِ صریحِ کاربر)

- **منبع: CLIِ رسمیِ خودِ تکنولوژی + یک لایهٔ نازکِ چسب.** فایل‌ها را
  دست‌نویس نمی‌کنیم. چسب کاری را می‌کند که هیچ CLIای انجامش نمی‌دهد: متغیرهای
  env و اضافه‌کردنشان به `.env.example`، وصل‌کردنِ app به مونوریپو، پورت‌ها و
  آدرس‌های داخلی، و ثبتِ تصمیم در `docs/decisions/` و `project.config.json`.
- **عمق: کدِ کارکندهٔ قابلِ اجرا.** نه پوشهٔ نیمه‌کاره. با یک استثنای
  صادقانه: برای **تعویضِ** فریم‌ورکِ یک پروژهٔ موجود، ابزار صریحاً می‌گوید
  «نمی‌توانم دکمه‌ای عوض کنم» — ادعای خلافش همان خطای نسخهٔ قبلی است.
- **هر تصمیم = یک commitِ جدا** با تریلرِ `PackageBuilder-Decision`.

### ج) برگشت با **بازسازی**، نه `git revert` (اصلاحیهٔ تصمیمِ ۰۰۰۳)

نگارشِ اول `git revert` گفته بود؛ **در عمل جواب نداد** و تستِ قدمِ ۷ نشانش
داد. تصمیم‌ها فایلِ مشترک دارند (`.env.example`، `docker-compose.yml`،
`project.config.json`)، پس برگشتِ یک تصمیمِ قدیمی‌تر تضاد می‌خورد و دکمهٔ
پشیمانی فقط برای **آخرین** تصمیم کار می‌کرد.

روشِ فعلی — سه جنسِ فایل، سه راهِ برگشت:

| جنس | مثال | راهِ برگشت |
|---|---|---|
| ساخته‌شده از تصمیم‌ها | `docker-compose.yml` | از رویِ stackِ جدید **از نو ساخته** می‌شود |
| انباشته با نشانِ ما | بلوکِ `# ---- PostgreSQL ----` در `.env.example` | همان بلوک برداشته می‌شود |
| بیرونی | `node_modules`، خروجیِ CLIهای رسمی | فرمانِ `remove` در رجیستری |

**نقشِ گیت عوض شد، حذف نشد:** همچنان تاریخ و تورِ ایمنی است، و روی درختِ
کثیف برگشت **امتناع** می‌کند تا کارِ کامیت‌نشدهٔ کاربر نپرد. و چیزی که خودکار
برنمی‌گردد (مثلِ `apps/web`ِ یک فریم‌ورک) `remove.manual` دارد و ابزار
می‌گوید «این را خودت باید برداری» — نه اینکه ادعای حذف کند و آشغال بگذارد.

تکنولوژی‌هایی که برگشتِ دستی دارند: `node`, `pnpm`, `npm`,
`react-router-v7`, `nextjs`, `nestjs`.

---

## ۶. رجیستری — قلبِ عمومی‌بودن

هر تکنولوژی یک **رکوردِ داده** است، نه کدِ پراکنده:

```
{ شناسه، دستهٔ تصمیم، دستورِ CLIِ رسمی (حالتِ غیرتعاملی)،
  لایهٔ چسب (env، مسیرها، اتصال به مونوریپو)،
  مدرکِ تشخیص، ناسازگاری‌ها، پیش‌نیازها }
```

اضافه‌کردنِ تکنولوژیِ نو یعنی اضافه‌کردنِ یک ردیف، نه دست‌بردن در کدِ ابزار.

| دسته | گزینه‌ها |
|---|---|
| `language` | Node.js / Python |
| `packageManager` | pnpm / npm |
| `monorepoTool` | Turborepo / Nx |
| `frontendFramework` | React Router v7 (framework mode) / Next.js |
| `backendFramework` | NestJS / Express |
| `backgroundJobs` | BullMQ |
| `database` | PostgreSQL / MySQL |
| `search` | Meilisearch / Elasticsearch |
| `storage` | MinIO / S3 (ابری) |
| `e2e` | Playwright / Cypress |

**۱۰ دسته، ۱۹ تکنولوژی، هر ۱۹ تا `verified: true`.**

دامنهٔ دورِ اول عمداً «در هر دسته دو گزینه» بود — یکی که می‌دانیم کار می‌کند و
یک رقیب — برای اثباتِ عمومی‌بودنِ ماشین. رساندنش به سه-چهار گزینه فقط «یک
ردیفِ داده اضافه کن» است.

### تشخیص هم داده است، نه کد

انواعِ مدرک، ترکیب‌شدنی: `file`, `npm`, `npmRoot`, `npmInstalled`,
`pythonVenv`, `dockerService`, `envVar`, و دو ترکیب‌گرِ `all` / `any`.

**«نامعلوم» در ترکیب‌ها سرایت می‌کند:** اگر `all` یک زیرشرطِ نامعلوم داشته
باشد و بقیه درست باشند، نتیجه **نامعلوم** است، نه درست.

### انواعِ گامِ اعمال

`cli`, `env`, `writeFile`, `mkdir`, `pnpmAddDev`, `composeService`, `file` —
و برای حذف: `cli`, `deleteFile`, `composeDown`.

### دو چیدمان پشتیبانی می‌شود

`resolveRole` هم مونوریپو (`apps/web`) و هم تک‌پکیجی (خودِ ریشه) را می‌فهمد.
ابزار هیچ‌کدام را تحمیل نمی‌کند.

### نگهبانِ صداقت

`tests/registry.test.mjs` فهرستِ تأییدشده‌ها را **صریح** نگه می‌دارد: اگر
تکنولوژیِ نویی بی‌آزمایش `verified` بگیرد، تست قرمز می‌شود. UI هم برچسبِ
«نصبش آزمایش‌نشده» را نشان می‌دهد.

---

## ۷. ترمینالِ واقعی و سنتینل — سخت‌ترین بخشِ فنی

یک PowerShell 7 واقعیِ همیشه‌زنده در همان صفحه. کاربر هم می‌تواند خودش
داخلش تایپ کند. سه درسِ سختی که فقط با اجرای زنده پیدا شدند:

### درسِ ۱ — نشانه بینِ دو تکهٔ خروجی می‌شکند

خروجیِ PTY تکه‌تکه می‌رسد و مرزش تصادفی است: `##STEP_` در یک تکه و
`OK:id##` در تکهٔ بعد. اگر هر تکه جدا بررسی شود، خبرِ پایان **هرگز** نمی‌رسد و
وضعیت برای همیشه گیر می‌کند. حل: بافرِ کوچکِ کران‌دار +
`couldStartSentinel()`. تست: همان متن با هر تکه‌بندی، از حرف‌به‌حرف تا یک‌جا.

### درسِ ۲ — پژواکِ فرمان خبرِ جعلی می‌ساخت

پاورشلِ تعاملی خطِ فرمان را پژواک می‌کند. چون نشانه عیناً در متنِ فرمان بود،
تجزیه‌گر آن را در پژواک می‌دید — *قبل از اجرای فرمان*. اندازه‌گیریِ واقعی:
**سه خبر برای یک فرمان** (true, false, true) و اولینش «موفق». یعنی فرمانِ
شکست‌خورده، موفق گزارش می‌شد.

حل: تابعِ `__pbEnd` یک‌بار در آغازِ پوسته تعریف می‌شود و نشانه را در **زمانِ
اجرا** سرِهم می‌کند، پس خطِ فرمان نه `##` دارد نه `STEP_`.

### درسِ ۳ — `$?` در پاورشل برای فرمانِ بیرونی دروغ می‌گوید

`$?` برای فرمانی که روی stderr بنویسد `false` می‌شود، و pnpm نوارِ پیشرفتش را
روی stderr می‌نویسد. یعنی **نصبِ موفق «شکست» گزارش می‌شد** — آینهٔ باگِ ۲.

حل: مقدارِ کاوشیِ ۹۹۹ در `$LASTEXITCODE`، تا فرمانِ بیرونی از cmdlet تفکیک
شود. به‌علاوهٔ `try/catch` تا خطای قطع‌کننده هم سنتینل را رد نکند (وگرنه
وضعیت برای همیشه گیر می‌ماند).

شکلِ نهایی در `src/server/sentinel.mjs`:

```js
const SENTINEL_PROBE = 999;
export const SETUP_COMMAND =
  "function __pbEnd { param($id, $success, $code) " +
  `$ok = if ($code -eq ${SENTINEL_PROBE}) { $success } else { $code -eq 0 }; ` +
  'Write-Host ("#"*2 + "STEP_" + $(if ($ok) { "OK" } else { "FAIL" }) + ":" + $id + "#"*2) }';

export function withSentinel(command, stepId) {
  if (!/^[A-Za-z0-9_-]{1,64}$/.test(stepId)) throw new Error(`شناسهٔ مرحله نامعتبر است: ${stepId}`);
  return (
    `$global:LASTEXITCODE = ${SENTINEL_PROBE}; ` +
    `try { ${command}; __pbEnd ${stepId} $? $LASTEXITCODE } ` +
    `catch { __pbEnd ${stepId} $false 1 }`
  );
}
```

نکتهٔ دیگر: `createTerminal({ useConpty: false })` — با ConPTY خطای
`AttachConsole failed` می‌داد؛ winpty جواب می‌دهد.

---

## ۸. امنیت — چرا «فقط لوکال است» کافی نیست

سرور می‌تواند فرمان اجرا کند، یعنی دسترسیِ کاملِ این کامپیوتر. هر تبِ
مرورگری که باز باشد می‌تواند به `127.0.0.1` درخواست بفرستد. (نسخهٔ قبلیِ
ابزار `GET /run?command=...` داشت، بی هیچ محافظتی.)

| لایه | چه چیزی را می‌بندد |
|---|---|
| فقط `127.0.0.1` | دسترسی از شبکه |
| فقط `POST` | حملهٔ `<img src=...>` و لینکِ ساده |
| توکنِ تصادفیِ هر-اجرا، تزریق‌شده در صفحهٔ خودمان | صفحهٔ بیگانه نمی‌تواند بخواندش |
| بررسیِ `Origin` در مقابلِ `Host` | درخواستِ آمده از مبدأِ دیگر |

به‌علاوه: `timingSafeEqual` برای مقایسهٔ توکن، فهرستِ سفید برای سرو کردنِ
فایل‌های vendor، و ارتقای WebSocket هم زیرِ همان توکن و مبدأ.
`tests/server.test.mjs` هر چهار لایه را می‌کوبد: بی‌توکن → ۴۰۱، توکنِ غلط →
۴۰۱، `GET` → ۴۰۴.

---

## ۹. ⭐ باگ‌هایی که فقط اجرای واقعی پیدا کرد

**این مهم‌ترین درسِ کلِ پروژه است:** تستِ واحد با تقلبی (fake) نمی‌تواند باگِ
مرزی با ابزارهای بیرونی را بگیرد. هر فازْ باگ‌های واقعی‌اش را از اجرای زنده
گرفت، نه از ۲۰۵ تستِ سبز.

### قدمِ ۷ — چهار باگ

| # | باگ | چرا مهم بود |
|---|---|---|
| ۱ | composeِ تولیدی `volume` را ارجاع می‌داد ولی در بخشِ بالای `volumes` تعریف نمی‌کرد | Docker: «refers to undefined volume» |
| ۲ | دانشِ هر ایمیج جا افتاده بود | `POSTGRES_PASSWORD` (بی آن کانتینر بالا نمی‌آید)، مسیرِ دادهٔ درست (`/var/lib/postgresql/data` نه `/data`)، `command`ِ لازمِ MinIO |
| ۳ | پورتِ ثابت همیشه تصادم می‌کرد | **خطرناک‌ترین:** سرویس بالا می‌آمد **بی نگاشتِ پورت**، یعنی دیتابیسی که از میزبان در دسترس نبود — و ابزار «موفق» گزارشش می‌کرد |
| ۴ | `.env` جای اشتباه خوانده می‌شد | با `-f <path>` خودِ Docker `.env` را از پوشهٔ فایلِ compose می‌خواند، نه ریشهٔ پروژه. حل: `--env-file` صریح در هر فراخوانی |

نکتهٔ باگِ ۳: مرجعِ «آزاد بودنِ پورت» خودِ `docker ps` است، نه تلاشِ bind —
چون روی ویندوز bind روی پورتِ گرفته‌شدهٔ Docker همیشه خطا نمی‌دهد و probe
دروغ می‌گفت. حالا بعدِ بالا آمدن، انتشارِ واقعیِ پورت هم چک می‌شود.

### تأییدِ نهایی — هفت باگِ دیگر

هر ۱۹ ردیف یک‌بار روی پوشهٔ خالی اجرا و با مدرکِ واقعی سنجیده شد:

| # | باگ | چرا مهم بود |
|---|---|---|
| ۱ | تشخیصِ Node، `node_modules` را هم شرط کرده بود | `npm init -y` آن را نمی‌سازد، پس Node **هرگز** «نصب» نمی‌شد و **کلِ زنجیره** قفل می‌ماند |
| ۲ | تشخیصِ Python همان اشکال را داشت | `python -m venv` هیچ manifestای نمی‌سازد |
| ۳ | `pnpm add -Dw` بیرونِ workspace خطا می‌دهد | ابزارهای ریشه در پروژهٔ تک‌پکیجی نصب نمی‌شدند |
| ۴ | `$?` برای فرمانِ بیرونی دروغ می‌گوید | نصبِ موفق «شکست» گزارش می‌شد (درسِ ۳ بالا) |
| ۵ | خطای قطع‌کننده سنتینل را رد می‌کرد | وضعیت **برای همیشه** روی «در حالِ اجرا» گیر می‌کرد |
| ۶ | pnpm ۱۱ نصب را با `[ERR_PNPM_IGNORED_BUILDS]` می‌شکند | nx و cypress نصب می‌شدند ولی «شکست» گزارش می‌شد |
| ۷ | `create-next-app` توقع دارد پوشهٔ والد باشد | پیامش گمراه‌کننده بود: «path is not writable» |

باگِ ۱ درسِ مفهومی داشت: تعریفم غلط بود. «زبانِ پروژه Node است» یعنی
`package.json` دارد؛ **نصبِ وابستگی‌ها سؤالِ جداگانه‌ای است.** یعنی:

```js
detect: { kind: "file", path: "package.json" },
apply: { verified: true, steps: [{ kind: "cli", command: "npm init -y" }] },
```

دربارهٔ باگِ ۶: اولین تلاشم کلیدِ pnpm ۱۰ را نوشت (`onlyBuiltDependencies`).
خودِ pnpm بود که `allowBuilds: nx: set this to true or false` را نوشت و کلیدِ
درست را نشان داد. به‌علاوهٔ `strictDepBuilds: false` برای وابستگی‌های
غیرمستقیمِ نادانستنی.

### سه شکافِ طراحی

- **Express و BullMQ** فرض می‌کردند `apps/api` و `apps/worker` از قبل هستند،
  ولی هیچ‌چیز آنها را نمی‌ساخت. حالا خودشان یک appِ حداقلی می‌سازند (این دو
  CLIِ رسمی ندارند، پس استثنای مستندِ تصمیمِ ۰۰۰۳اند).
- **فریم‌ورک‌ها** به `pnpm-workspace.yaml` نیاز دارند تا نصب از ریشه appشان
  را ببیند. حالا خودشان تضمینش می‌کنند.
- **`pnpm create` / `pnpm dlx`** روی این سیستم می‌شکست
  (`Cannot find package 'semver'`). روشِ کارکننده:
  `npx --yes ... --no-install` و بعد نصب از ریشهٔ workspace — که برای
  مونوریپو هم روشِ درست‌تری است.

الگوی نهاییِ هر سه اسکافولدر:

```js
{ kind: "writeFile", path: "pnpm-workspace.yaml", content: WORKSPACE_YAML },
{ kind: "mkdir", path: "apps" },
{ kind: "cli", command: "npx --yes create-react-router@latest apps/web --yes --no-install" },
{ kind: "cli", command: "pnpm install" },
```

### باگ‌های خودم که کاربر پیدا کرد

- **`apps/ai-service` را «node» برچسب زده بودم** — هم `package.json` داشت هم
  `requirements.txt`. حل: `kinds` چندمقداری (`node+python`).
- **تناقضِ خودساخته در UI:** وضعیتِ «وابستگی‌های ریشه» را از متنِ نثرِ مدرک
  با تطبیقِ رشته درمی‌آوردم؛ الگو نمی‌گرفت و صفحه «هر دو هست» را بالای
  «`node_modules` نیست» نشان می‌داد. حل: `probe.rootDeps` به‌عنوانِ دادهٔ
  واقعیِ سرور + یک تستِ سازگاری.
- **دکمه‌ها قبل از وصل‌شدنِ ترمینال فعال بودند** — فرمان اجرا می‌شد و
  خروجی‌اش دیده نمی‌شد، **نقضِ قاعدهٔ ۲**. حالا `termReady` گیتشان می‌کند.
- **سه ایرادِ خوانایی:** سلسله‌مراتبِ appها دیده نمی‌شد؛ برچسبِ جنسِ app
  رنگِ سبزِ وضعیت داشت پس «هست» خوانده می‌شد؛ و «نبودنِ یک ردیف» که یعنی
  «مربوط نیست»، نامرئی بود.

### باگ‌های تقلبی‌های تست (دو بار!)

تقلبیِ Docker فیلدهایی داشت که کدِ واقعی تولید نمی‌کند (`composeFileExists`,
`checked`) — تست‌ها سبز بودند ولی ابزار روی دادهٔ واقعی «نیست» را «نامعلوم»
می‌گفت. بارِ دوم تقلبیِ پورت فقط ۵۳۰۰–۵۹۹۹ را پوشش می‌داد و برای MinIO
(۹۰۰۰) می‌شکست. حل: سه تستِ **قرارداد** که تقلبی را با خروجیِ واقعی مقایسه
می‌کنند.

---

## ۱۰. نکاتِ عملیِ محیط (ویندوز)

- **دامنه‌بندیِ Docker Compose با فیلدِ `name:` است، نه مسیرِ پوشه.**
- `--env-file` در **هر** فراخوانیِ compose لازم است (باگِ ۴ قدمِ ۷).
- `useConpty: false` برای node-pty.
- مرجعِ اشغالِ پورت: `docker ps`، نه `net.createServer`.
- **PTYِ باز، حلقهٔ رویدادِ Node را زنده نگه می‌دارد.** تست‌ها ۲۴۰ ثانیه
  هنگ کردند تا یک نگهبانِ `unref`شدهٔ خروجِ اجباری در `after()` اضافه شد.
  در `cli.mjs` هم `runApplyOrRevert` عمداً `process.exit(0)` صدا می‌زند.
- خروجیِ ترمینال فقط به کلاینت‌های **وصل** می‌رسد؛ گرم‌شدنِ ۹۰۰ms قبل از
  اتصالِ خروجی لازم بود.
- پاورشل خطِ بلندِ ورودی را با نیوـ‌لاینِ واقعی می‌شکند، پس تستِ متنِ ترمینال
  باید فاصله‌ها را حذف کند (`replace(/\s+/g, "")`).

---

## ۱۱. تست‌ها

**۲۰۵ تستِ واحد** (~۲۰ ثانیه) در ۸ فایل:

| فایل | پوشش |
|---|---|
| `scaffold.test.mjs` | پوشهٔ ناموجود/خالی/پُر، مسیری که فایل است، زبالهٔ سیستم‌عامل، `--dry-run`، slugِ نامعتبر، و اینکه هیچ فایلِ تحمیل‌کنندهٔ تکنولوژی ساخته نمی‌شود |
| `detect.test.mjs` | سه‌حالتی، سرایتِ نامعلوم، `kinds` چندمقداری، Dockerِ خاموش (با تزریق) |
| `registry.test.mjs` | اعتبارسنجیِ داده + ۳ تستِ **قرارداد** + نگهبانِ صداقتِ ۱۹ تکنولوژی |
| `sentinel.test.mjs` | تکه‌بندی (تا حرف‌به‌حرف)، پسرفتِ پژواک، معیارِ کدِ خروج، `try/catch` |
| `terminal.test.mjs` | موفقیت با stderr، شکستِ cmdlet، موفقیتِ cmdlet، مرگِ پوسته |
| `server.test.mjs` | هر چهار لایهٔ امنیت |
| `apply.test.mjs` | تولیدِ compose، env، پورت‌ها، ثبتِ تصمیم، برگشت |
| `independence.test.mjs` | **اجبارِ قاعدهٔ ۶** — مسیرِ مطلق، نامِ پروژهٔ همسایه، وابستگیِ بیرونِ فهرستِ سفیدِ ۵ تایی |

**۷ تستِ Playwright** روی مرورگرِ واقعی — و عمداً به ظاهرِ صفحه بسنده
نمی‌کنند: خودشان `docker ps` می‌زنند و `pg_isready` را داخلِ کانتینر اجرا
می‌کنند. یعنی سبزبودنِ تست، مدرکِ واقعی است.

هر تستِ تغییردهنده پوشه و دامنهٔ Dockerِ خودش را دارد (`freshProject(slug)`)
— بی این، تستی که تنها پاس می‌شد در اجرای گروهی می‌شکست.

تله‌های تستی که خوردیم و ارزشِ یادآوری دارند: انتخاب‌گرِ مبهمِ `.warn-tag`،
تستِ لرزانِ توکن (`token.slice(0,-1)+"0"` می‌توانست خودِ توکن شود)، و
`includes("DATABASE_URL")` که کامنتِ توضیحیِ خودِ قالب را می‌گرفت (به
`/^DATABASE_URL=/m` عوض شد).

---

## ۱۲. روشِ کار با این کاربر — مهم

اینها از تجربهٔ همین پروژه‌اند و رعایتشان لازم است:

- **«ادامه بده» یعنی «از جایی که قطع شدی ادامه بده»، نه تأیید.** یک‌بار آن را
  تأییدِ تصمیم گرفتم و قدمِ بعد را شروع کردم؛ تصحیح شدم: «قدم ۲ رو من نمی‌دونم
  چیه؟ گفتی توافق کردیم… اگه سوالی داری بپرس همین طوری نرو قدم بعدی.»
- **آخرِ هر قدم بایست و تأیید بگیر** — مخصوصاً برای تصمیم‌های معماری.
- **سریع جلو نرو.** درخواستِ صریحِ کاربر: «step by step / سریع جلو نرو کم کم».
- **عامیانه توضیح بده.** چند بار خواسته شد: «نمی‌فهمم ساده بگو عامیانه»،
  «من دیگه نمی‌فهمم چیکار می‌کنی». توضیحِ غیرفنی، نه اصطلاح‌بازی.
- **تستِ Playwright جای وارسیِ دستیِ مرورگر است** — تصمیمِ خودِ کاربر: یک‌بار
  تست بنویس، بعد شکست‌ها را مستقیم ببین.
- **پوشهٔ `- Sources` در پروژهٔ TaminLibrary دادهٔ پروژه نیست** — محلِ موقتِ
  خودِ کاربر است؛ دست نزن، وارد نکن، کامیت نکن.
- **اسنادِ تصمیم با اصلاحیه اصلاح می‌شوند، نه بازنویسیِ تاریخ.**
- **پروژه‌های واقعیِ روی این کامپیوتر فقط برای آزمایشِ خواندنی‌اند** و هرگز
  دست‌کاری نمی‌شوند. کانتینرهای `n00-*` و `nilalibrary002-*` مالِ کاربرند.

---

## ۱۳. بیرونِ محدوده (فعلاً)

عمداً جزوِ نقشه نیستند تا کار پخش نشود: انتشار/دیپلوی خودکار، تکنولوژی‌های
بیشتر از دامنهٔ دورِ اول، و هر لایهٔ هوشِ مصنوعی.

---

## ۱۴. اگر بخواهی کارِ نو شروع کنی

نقطه‌های طبیعیِ ادامه:

1. **گزینهٔ سوم و چهارم در هر دسته** — الان یک ردیفِ داده است، ولی هر ردیفِ
   نو باید **واقعاً اجرا شود** وگرنه نگهبانِ صداقت قرمز می‌شود.
2. **صفِ کار (`backgroundJobs`) فقط یک گزینه دارد** — رقیبش خالی است.
3. **پوششِ لینوکس/مک** — سنتینل و `detectShell` الان پاورشل‌محورند.
4. **`docs/decisions/` در پروژه‌های تولیدی** — سندهای تولیدی ساده‌اند و
   می‌توانند غنی‌تر شوند.

**قبل از هر کاری:** `npm test` و `npm run e2e` را بزن تا مطمئن شوی پایه سبز
است. و یادت باشد: **تستِ سبز، مدرکِ کارکردن با ابزارِ بیرونی نیست.** هر
تغییری که مرزِ بیرونی را لمس می‌کند، باید یک‌بار روی یک پوشهٔ خالیِ واقعی
اجرا شود.
