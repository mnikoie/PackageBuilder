# CLAUDE.md — دستورالعملِ PackageBuilder

## این پروژه چیست

ابزارِ لوکالِ ساختِ پروژه: یک پوشهٔ خالی می‌گیرد و قدم‌به‌قدم، با انتخابِ
کاربر، تبدیلش می‌کند به پروژه‌ای آمادهٔ کار. از صفر نوشته می‌شود چون نسخهٔ
قبلی (`25- TaminLibrary/tools/project-creator`) روی یک قالبِ تک‌استک بنا شده
بود و عمومی‌شدنش شکست خورد.

## اول این‌ها را بخوان

1. `docs/decisions/0001-purpose-and-rules.md` — پنج قاعدهٔ سخت و دلیلشان.
2. `docs/ROADMAP.md` — هشت قدم و ترتیبشان.
3. `docs/decisions/` — بقیهٔ تصمیم‌ها و وضعیتشان.

## قواعدِ ثابت (نقض نکن)

- **بی‌مدرک ادعا نکن.** «نصب است» فقط با مدرکِ واقعی: پوشه در `node_modules`،
  خروجیِ `docker compose ps`، وجودِ `.venv`. حضورِ نام در `package.json` یا
  `docker-compose.yml` **مدرک نیست**. سه حالت: هست / نیست / **نامعلوم**.
  «نامعلوم» هرگز نباید به «نیست» یا «هست» ترجمه شود.
- **هیچ چیزی پنهان اجرا نشود.** هر دستور در ترمینالِ واقعیِ قابلِ دیدن.
- **هیچ تصمیمی تحمیل نشود.** فایلِ یک تکنولوژی فقط بعد از انتخابِ آن. این
  قاعده شاملِ خودِ این پروژه هم می‌شود.
- **تصمیمِ «پیشنهادی» مبنای کد نیست.** تا ✅ نشود، کدی بر مبنایش نوشته نمی‌شود.
- **هر کاری دو طرفه.** هر چه نصب می‌شود باید حذف شود، و بعدِ حذف در فهرست
  بماند (نصب‌نشده)، نه ناپدید.
- **قبل از پاک/بازنویسی، داخلش را نگاه کن.**

## روشِ کار

- **قدم‌به‌قدم.** آخرِ هر قدمِ ROADMAP بایست، خروجی را نشان بده، منتظرِ تأیید
  بمان. جلوتر نرو.
- **قبل از گفتنِ «انجام شد»، خودت تست کن** — حالتِ خالی، حالتِ کامل، حالتِ
  خطا. گزارشِ آزمایش‌نشده قابلِ قبول نیست.
- منطق را از UI جدا نگه دار، تا با عوض‌شدنِ UI دست‌نخورده بماند.
- مرتب commit بزن.

## منابع

`D:\- AIProject\25- TaminLibrary` منبعِ سورس و مرجعِ «یک استکِ کاملِ
کارکننده» است. از آن می‌شود کد برداشت، ولی **دست‌کاری‌اش نکن**.
