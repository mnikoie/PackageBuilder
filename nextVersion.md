# nextVersion.md — کارهایی که برای نگارشِ بعدی مانده

> اینجا چیزهایی نوشته می‌شود که **می‌دانیم ناقص‌اند** ولی الان اولویت نبوده‌اند.
> نوشتنشان از فراموش‌کردنشان بهتر است — و از تظاهر به اینکه وجود ندارند، خیلی بهتر.

---

## ۱. ✅ حل شد — کانتینرِ خاموشی که بعدِ «برگشت» جا می‌ماند

### ساده بگویم چه اتفاقی می‌افتد

فرض کن روی دکمهٔ «نصبِ PostgreSQL» می‌زنی. سه چیز اتفاق می‌افتد:

1. اسمِ postgres در فایلِ `docker-compose.yml` نوشته می‌شود — این فایل مثلِ
   **فهرستِ خرید** است: چه سرویس‌هایی جزوِ این پروژه‌اند.
2. Docker از روی آن فهرست یک **کانتینر** می‌سازد و راهش می‌اندازد — کانتینر
   یعنی خودِ آن دیتابیسِ زنده که دارد کار می‌کند.
3. دیتابیس بالا می‌آید و آماده است.

حالا پشیمان می‌شوی و «برگشت» را می‌زنی. ابزار دو کار می‌کند:

1. کانتینر را **می‌خواباند** (`stop`) — یعنی خاموشش می‌کند، ولی پاکش نمی‌کند.
   مثلِ ماشینی که خاموش کرده‌ای و در پارکینگ گذاشته‌ای.
2. اسمِ postgres را از **فهرستِ خرید** برمی‌دارد.

و اینجا مشکل زاده می‌شود: آن کانتینرِ خاموش هنوز روی کامپیوترت هست، ولی
اسمش دیگر در فهرست نیست. یعنی از این به بعد **هیچ‌کس صاحبش نیست**. Docker به
چنین چیزی می‌گوید کانتینرِ **یتیم**. حتی فرمانِ پاک‌سازیِ استاندارد
(`docker compose down`) هم پیدایش نمی‌کند، چون آن فرمان فقط چیزهایی را که در
فهرست هستند پاک می‌کند.

### چرا مهم است

زیانِ فوری ندارد — خاموش است و کاری نمی‌کند. ولی:

- با هر بار نصب-و-برگشت، یکی-دو تا اضافه می‌شود و روی هم تلنبار می‌شوند.
- جای دیسک می‌گیرند (هر کدام همراهِ داده‌اش).
- اسمشان را قُرُق می‌کنند، پس دفعهٔ بعد ممکن است سرِ نامِ تکراری تصادم شود.
- و از همه مهم‌تر: **قاعدهٔ چهارمِ خودِ ما** می‌گوید «هر کاری دو طرفه باشد».
  الان تقریباً دو طرفه است، نه کاملاً. یعنی ابزار در ادعایش کمی از واقعیت
  جلوتر است — و همین دقیقاً همان چیزی است که این پروژه برای رفعش نوشته شد.

### چطور پیدا شد

اجرای واقعیِ `npm run e2e` در ۱۴۰۵/۰۵/۲۸ (۲۰۲۶-۰۸-۱۹). هر ۷ تست سبز بودند،
ولی بعدِ اجرا دو کانتینرِ خاموش روی سیستم مانده بود:
`clickedapp-postgres-1` و `terminalvisible-postgres-1`. دستی پاکشان کردم.
باز هم همان درسِ همیشگی: **تستِ سبز، مدرکِ تمیزی نیست.**

### راهِ حل (برای وقتی که سراغش رفتیم)

سه گزینه، از تمیز به کثیف:

1. **ترتیب را عوض کن:** *اول* کانتینر را کامل بردار، *بعد* اسمش را از فهرست
   حذف کن. تا وقتی در فهرست است، Docker می‌داند مالِ کیست.
2. **`stop` را به `down` ارتقا بده** برای همان یک سرویس، تا کانتینر واقعاً
   برداشته شود نه فقط خوابانده. اینجا یک سؤال هست که باید از کاربر پرسید:
   **دادهٔ داخلش چه شود؟** خوابیدن داده را نگه می‌دارد؛ برداشتنِ کامل با
   `-v` داده را هم می‌برد. پیشنهاد: کانتینر برداشته شود ولی دادهٔ ذخیره‌شده
   (volume) بماند، و اگر کاربر دادهٔ او را هم خواست، صریح بپرسیم.
3. **حداقلِ کار (اگر هیچ‌کدام):** ابزار صریح بگوید «کانتینرِ فلان خاموش شد
   ولی روی سیستم ماند؛ اگر خواستی خودت پاکش کن: `docker rm ...`». این کمترین
   کاری است که صداقت را حفظ می‌کند.

**جای کد:** `src/core/apply.mjs`، گامِ `composeDown` (حدودِ خطِ ۸۱۲) که الان
`docker compose ... stop <service>` می‌زند. تستِ پاک‌سازیِ e2e هم در
`e2e/build-a-project.spec.mjs`، بلوکِ `test.afterAll`.

**قبل از «انجام شد» گفتن:** یک‌بار نصب و برگشتِ واقعی، و بعدش `docker ps -a`
که نشان دهد چیزی جا نمانده. تستِ سبز کافی نیست.

### ✅ چه شد (۱۴۰۵/۰۵/۲۸)

گزینهٔ ۱ و ۲ با هم اجرا شدند: گامِ `composeDown` حالا اول `stop` می‌زند و بعد
`rm -f` — یعنی کانتینر واقعاً برداشته می‌شود، **ولی عمداً بی `-v`**. دادهٔ
داخلِ سرویس مالِ کاربر است و پاک‌کردنش تصمیمِ او، نه ابزار. اگر همان سرویس را
دوباره نصب کند، داده‌اش سرِ جایش است.

مدرکِ واقعی، نه تستِ سبز: پشتهٔ Grafana+Loki+Prometheus نصب شد (هر سه کانتینر
`Up`)، بعد برگشت زده شد، و `docker ps -a` نشان داد **هیچ‌کدام نماندند**؛ در
همان حال `docker volume ls` هنوز `proj_grafana_data` و `proj_loki_data` و
`proj_prometheus_data` را داشت. یعنی دقیقاً همان چیزی که قرار بود: کانتینر
رفت، داده ماند.

---

## ۲. ✅ حل شد — تستِ سرتاسریِ «چرخهٔ کامل» که یک‌بار لرزیده بود

### چه بود

در یکی از اجراهای `npm run e2e` تستِ چرخهٔ کامل شکست خورد و بعد چهار اجرای
پیاپی سبز شد. مدرکِ آن لحظه از `test-results/` پاک شده بود، پس **نامعلوم**
ثبت شد — نه باگ، نه سالم. حدسِ آن‌وقت «کندیِ Docker زیرِ بار» بود.

### حالا مدرک هست

در اجرای بعد از افزودنِ پرسشِ رمز، همان تست دوباره شکست و این بار متنش را
داشتم:

```
Expected substring: "accepting connections"
Received string:    "/var/run/postgresql:5432 - no response"
```

حدس درست بود ولی دقیق نبود. مسئله کندیِ عمومی نبود؛ **«کانتینر بالاست» با
«Postgres آماده است» یکی نیست**. ایمیجِ Postgres در اولین اجرا دیتابیس را
می‌سازد و در آن چند ثانیه `pg_isready` جواب می‌دهد «no response». تست
بلافاصله بعد از `up` می‌پرسید.

پس اصلاً نوسانی نبود: هر وقت ساختِ اولیه کمی طول می‌کشید، قرمز می‌شد.

### چه شد

انتظار به حلقه تبدیل شد: تا ۴۰ بار می‌پرسد و بینشان یک ثانیه صبر می‌کند.
ولی خطای «رمز قبول نشد» بلافاصله بالا می‌آید و دوباره امتحان نمی‌شود — آن
شکستِ واقعی است، نه نبودنِ آمادگی. اگر این تفکیک نبود، حلقه یک باگِ واقعی را
هم چهل بار تکرار می‌کرد و آخرش «آماده نشد» می‌گفت.

### درسِ ماندگار

سرنخی که در همان یادداشت نوشته شده بود («مدرکِ همان لحظه را نگه دار») همان
چیزی بود که این بار حلش کرد. نامعلوم را نامعلوم گذاشتن — نه «حل شد» گفتن —
باعث شد دفعهٔ بعد دنبالِ متنِ خطا بگردم.

---

## ۳. ✅ حل شد — اعمالی که وسطِ راه شکست بخورد، ساکت بود

### چه دیدم

نصبِ Celery وقتی اجرا شد که Docker خواب بود. گام‌های اولش انجام شدند (پوشه،
فایل‌ها، محیطِ مجازی، نصبِ pip) و گامِ آخر — بالا آوردنِ Redis — شکست خورد.
ابزار درست گفت «شکست خورد»، ولی آنچه ساخته بود سرِ جایش ماند و درختِ گیت
کثیف شد. نتیجه‌اش دو دردسر:

1. برگشتِ **تکنولوژی‌های دیگر** قفل شد، چون برگشت روی درختِ کثیف امتناع
   می‌کند (که خودش قاعدهٔ درستی است).
2. اجرای بعدیِ همان نصب گفت «از قبل نصب است»، چون پکیجِ پایتون واقعاً آنجا
   بود — در حالی که سرویسش هرگز بالا نیامده بود.

### دومی همان‌جا رفع شد

مدرکِ Celery و BullMQ حالا **هر دو تکه** را می‌خواهد: خودِ پکیج و بروکرش.
کارگری که بروکر نداشته باشد اجرا نمی‌شود، پس «نصب است» با دیدنِ تنها یکی‌شان
دروغ بود.

### ولی اولی باز است

سؤالِ واقعی این است: **اعمالِ نیمه‌تمام باید برگردد یا بماند؟** هر دو جواب
عیب دارند. برگرداندنِ خودکار ممکن است کاری را که کاربر همان لحظه دستی درست
کرده پاک کند؛ ماندنش پروژه را در حالتِ نیمه‌کاره رها می‌کند.

پیشنهاد: نه پاک‌کردنِ خودکار، نه سکوت. ابزار در پایانِ اعمالِ شکست‌خورده
صریح فهرست کند چه چیزهایی ساخته شده‌اند و بگوید «اینها ماندند؛ یا خودت
پاکشان کن یا بعد از رفعِ مشکل دوباره اجرا کن». کمترین کاری که صداقت را حفظ
می‌کند و تصمیم را دستِ صاحبِ پروژه می‌گذارد.

### ✅ چه شد (۱۴۰۵/۰۵/۲۸)

همان پیشنهاد اجرا شد: **نه پاک‌کردنِ خودکار، نه سکوت.** حالا هر اعمالِ
شکست‌خورده فهرست می‌کند چه چیزهایی ساخته شده و مانده‌اند — پوشه، فایل،
فرمانی که اجرا شد، سرویسی که بالا آمد، متغیرهای env، و پورت‌هایی که انتخاب
و ذخیره شدند. فایلی که رد شده و فرمانی که شکست خورده عمداً شمرده نمی‌شوند،
چون آنها چیزی روی دیسک نگذاشته‌اند.

چرا پاک نمی‌کنیم: ممکن است کاربر همان لحظه دستی درستش کرده باشد. پس تصمیم
دستِ صاحبِ پروژه می‌ماند و ابزار فقط صادقانه گزارش می‌دهد.

مدرکِ زنده: پروژه‌ای ساختم با فایلِ compose دست‌ساز، بعد PostgreSQL را نصب
کردم. ابزار درست امتناع کرد (فایلِ کاربر را بازنویسی نمی‌کند)، تکهٔ لازم را
داد، و زیرش نوشت:

```
These were already done and are still there:
  • متغیرهای env که اضافه شدند: DATABASE_URL
  • پورت‌هایی که انتخاب و ذخیره شدند: POSTGRES_PORT=5434

Nothing was rolled back on purpose - your call: clean them up, or fix the
problem and run again.
```

(پورتِ ۵۴۳۴ هم خودش انتخاب شد، چون ۵۴۳۲ و ۵۴۳۳ هر دو مشغول بودند.)

دو تستِ واحد هم نگهبانش شدند.
